/**
 * Hook para páginas de relatório (impressão/PDF).
 * Com dark mode removido, este hook é um no-op mantido por compatibilidade.
 */
export function useReportMode() {
  // Dark mode removido — nenhuma ação necessária.
}