{
  "name": "BugReport",
  "type": "object",
  "properties": {
    "descricao": {
      "type": "string",
      "description": "Descri\u00e7\u00e3o do problema relatado"
    },
    "pagina": {
      "type": "string",
      "description": "P\u00e1gina/tela onde o erro ocorreu"
    },
    "prints": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "URLs dos prints anexados"
    },
    "status": {
      "type": "string",
      "enum": [
        "aberto",
        "em_analise",
        "resolvido"
      ],
      "default": "aberto",
      "description": "Status do relato"
    },
    "resposta_admin": {
      "type": "string",
      "description": "Resposta/coment\u00e1rio do admin sobre o relato"
    },
    "resposta_admin_date": {
      "type": "string",
      "format": "date-time",
      "description": "Data em que o admin respondeu ao relato"
    },
    "avaliacao": {
      "type": [
        "number",
        "null"
      ],
      "default": null,
      "description": "Avalia\u00e7\u00e3o do suporte (1 a 5 estrelas) dada pelo solicitante"
    },
    "avaliacao_date": {
      "type": "string",
      "format": "date-time",
      "description": "Data em que o solicitante avaliou o suporte"
    }
  },
  "required": [
    "descricao",
    "pagina"
  ],
  "rls": {
    "read": {
      "$or": [
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "created_by": "{{user.email}}"
        }
      ]
    },
    "create": {},
    "update": {
      "$or": [
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "created_by": "{{user.email}}"
        }
      ]
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}